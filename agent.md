# 作业 4 执行说明

## 1. 文档目的

本文件用于指导在当前仓库中完成“互联网应用开发技术-作业 4”。目标是把题目要求拆解成可执行的开发、集成、测试和交付步骤，避免遗漏关键内容。

## 2. 当前仓库基线

- 根目录下已有 `frontend/`，是迭代 1 的 React 在线书店前端。
- `backend/` 目录当前为空，需要新建 Spring Boot 后端工程。
- 当前前端书籍数据来自 `frontend/src/data/books.js`，个人信息页说明文字明确写着“仅更新前端本地状态，不与后端交互”，这与本次作业要求存在差距，需要调整。

## 3. 题目原始要求整理

### 3.1 必做功能

在迭代 1 的基础上开发后端 Spring Boot 工程，并实现与前端工程的集成：

1. 创建一个 Spring Boot 项目，并配置好与 MySQL 数据库的连接。
2. 数据库脚本可以使用课程给出的样例数据库设计方案和样例数据。
3. 定义 `User` 和 `Book` 两个实体类。
4. 为 `User` 和 `Book` 分别创建 `Repository` 接口。
5. 实现以下 3 个核心 API：
   - `POST /api/v1/users/register`
   - `GET /api/v1/books`
   - `GET /api/v1/book/{id}`
6. 使用 Postman 或 Insomnia 对 API 进行测试，确保功能正常。
7. 使用“作业答题纸.docx”对项目结构截图并说明，并对 Postman 测试结果进行截图说明。

### 3.2 作业定位

- 本次作业不是完全独立的新项目，而是迭代 2 的基础。
- 实现时应优先选择“可继续扩展”的结构，不要只为了通过作业而写成一次性代码。

### 3.3 提交要求

1. 使用课程提供的“作业答题纸.docx”作答。
2. 将前端工程和后端工程放在两个目录中。
3. 连同图片、文本文件、答题纸等辅助内容一起打包上传。
4. 题面原文要求压缩文件命名为 `学号-作业1.rar`。这是题面原文，若老师后续有更正说明，应以最新通知为准。

### 3.4 评分标准

- Spring Boot 工程结构合理，分层架构合理：2 分
- 使用 Spring Data JDBC 或 Spring Data JPA 实现数据库读写：1 分
- 3 个 API 功能正确，并能通过 Postman 展示：3 分

## 4. 不可遗漏的硬性约束

- 后端必须是 Spring Boot。
- 数据库必须接入 MySQL。
- 必须存在 `User` 和 `Book` 两个实体。
- 必须存在 `UserRepository` 和 `BookRepository`。
- 必须实现题面要求的 3 个 API，路径不要写错。
- 数据必须来自数据库，而不是继续直接读取前端本地静态文件。
- 必须有 API 测试证据。
- 必须有项目结构截图和说明。
- 工程结构要体现分层设计，否则会直接影响评分。

## 5. 推荐实现范围

### 5.1 后端最小合格范围

至少完成以下内容：

- Spring Boot 项目可启动
- 成功连接 MySQL
- `Book` 数据能从 MySQL 查询
- `User` 数据能写入 MySQL
- 3 个 API 均可用
- 前端至少接入书籍列表和书籍详情接口

### 5.2 前端集成最低要求

题目写明“实现与前端工程的集成”，因此不能只做纯后端演示。建议最低完成：

- `Book List` 页面改为通过后端 `GET /api/v1/books` 获取数据
- `Book Detail` 页面改为通过后端 `GET /api/v1/book/{id}` 获取数据

### 5.3 用户注册的前端处理建议

当前前端没有独立注册页，只有 `ProfilePage`。为了满足“注册接口”要求，有两种合理方案：

1. 新增独立注册页，并调用 `POST /api/v1/users/register`
2. 将当前 `ProfilePage` 改造成“个人信息/注册”入口，在保存时调用注册接口

若时间有限，推荐方案 2，但要同步修改页面文案，避免页面还写着“不会发送到后端”。

## 6. 推荐分层设计

为了满足“结构合理、分层合理”的评分点，后端建议至少采用以下结构：

```text
backend/
  src/main/java/.../
    controller/
    service/
    repository/
    entity/ 或 model/
    dto/
    config/
    exception/
  src/main/resources/
    application.yml
    schema.sql 或 data.sql 或独立脚本目录
```

建议职责如下：

- `controller`：只负责接收请求和返回响应
- `service`：承接业务逻辑
- `repository`：负责数据库访问
- `entity`：数据库实体映射
- `dto`：请求/响应数据对象，避免直接暴露实体细节
- `config`：CORS、数据库、序列化等配置

## 7. 任务拆解

### 7.1 阶段一：后端工程初始化

- 创建 Spring Boot 工程
- 引入 Web、MySQL Driver、JPA 或 JDBC 等依赖
- 配置 `application.yml` 或 `application.properties`
- 验证应用可启动、数据库可连接

### 7.2 阶段二：数据库准备

- 根据课程样例建立 MySQL 数据库
- 创建 `users`、`books` 表
- 导入样例数据，尤其是 `books` 的初始化数据
- 明确主键、字段类型、非空约束

### 7.3 阶段三：领域模型与仓储

- 创建 `User` 实体
- 创建 `Book` 实体
- 创建 `UserRepository`
- 创建 `BookRepository`
- 若使用 JPA，补充表名、主键、字段映射注解

### 7.4 阶段四：核心 API 实现

必须逐一实现并验证：

1. `POST /api/v1/users/register`
   - 接收用户注册信息
   - 保存到数据库
   - 返回成功响应

2. `GET /api/v1/books`
   - 返回书籍列表
   - 数据源必须是数据库

3. `GET /api/v1/book/{id}`
   - 根据 id 返回单本书详情
   - 对不存在的 id 建议返回 404

### 7.5 阶段五：前端接入

- 将首页书籍列表从本地静态数据切换为接口请求
- 将书籍详情页从本地静态数据切换为接口请求
- 如需要，保留搜索逻辑，但搜索应基于接口返回结果
- 处理加载态、空态、错误态，避免接口失败时页面直接崩溃
- 若集成注册接口，更新 `ProfilePage` 的文案和保存逻辑

### 7.6 阶段六：API 测试与截图

- 使用 Postman 或 Insomnia 测试 3 个 API
- 为每个 API 至少准备 1 张成功截图
- 建议额外准备 1 张异常场景截图，例如查询不存在的图书

### 7.7 阶段七：答题纸与提交

- 截图前后端项目结构
- 简要说明各层职责和关键文件
- 粘贴或截图 Postman 测试结果
- 检查压缩包内是否包含 `frontend/`、`backend/`、答题纸及辅助文件

## 8. 测试清单

### 8.1 后端启动检查

- Spring Boot 应用可正常启动
- 应用启动后无数据库连接错误
- 数据库表已创建或可正常访问

### 8.2 API 功能测试

#### `POST /api/v1/users/register`

- 传入合法用户数据时返回成功
- 数据库中可看到新增用户记录
- 返回体包含可用于说明注册成功的信息

#### `GET /api/v1/books`

- 返回 HTTP 200
- 返回值为书籍数组
- 数组内容来自数据库
- 返回字段足够支持前端列表展示

#### `GET /api/v1/book/{id}`

- 传入存在的 id 时返回 HTTP 200
- 返回单本书详情
- 返回字段足够支持前端详情页展示
- 传入不存在的 id 时应有明确失败响应，推荐 404

### 8.3 前端集成测试

- 打开书籍列表页时可看到后端返回的数据
- 点击某本书进入详情页时，可看到该书的后端详情数据
- 后端关闭或请求失败时，页面能展示错误提示或空态，而不是白屏
- 若集成注册功能，提交后能成功写入数据库

### 8.4 回归测试

- 原有路由仍可访问
- 购物车本地逻辑未被破坏
- 页面基本样式未明显退化

## 9. 验收标准

满足以下条件可视为达到作业要求：

- 有可运行的 Spring Boot + MySQL 后端
- 有 `User`、`Book` 实体及对应 Repository
- 3 个核心 API 均已实现
- API 已通过 Postman/Insomnia 测试
- 前端已至少接入书籍列表和书籍详情
- 工程具备清晰分层
- 答题纸中有项目结构说明和测试截图
- 提交物组织完整

## 10. 明确非目标

以下内容不是题面硬性要求，没有时间时不要喧宾夺主：

- 完整登录鉴权
- 支付流程
- 订单系统
- 购物车持久化到后端
- 管理员后台
- 复杂搜索、分页、排序

这些可以作为迭代 2 的扩展，但不应妨碍本次核心目标完成。

## 11. 当前仓库的具体实施提醒

- `frontend/src/data/books.js` 当前是静态数据源，完成作业时不应再作为书籍列表和详情的最终真实来源。
- `frontend/src/pages/ProfilePage.jsx` 当前文案写明“不与后端交互”，如果把它用于注册，必须改文案。
- `backend/` 当前为空，建议将完整 Spring Boot 工程放入该目录，保持题目要求中的“前后端两个目录”结构。
- `frontend/作业答题纸.docx` 已存在，可直接基于该文件补充截图和说明。

## 12. 建议执行顺序

1. 先完成后端项目初始化和数据库连接
2. 再完成实体、Repository、Service、Controller
3. 先用 Postman 打通 3 个 API
4. 再把前端书籍列表和详情改为请求后端
5. 最后整理答题纸、截图和压缩包

## 13. 最终交付检查单

- [ ] `backend/` 中存在可运行的 Spring Boot 工程
- [ ] 已配置 MySQL 连接
- [ ] 已创建 `User` 和 `Book` 实体
- [ ] 已创建对应 Repository
- [ ] 已实现 `POST /api/v1/users/register`
- [ ] 已实现 `GET /api/v1/books`
- [ ] 已实现 `GET /api/v1/book/{id}`
- [ ] 已完成 Postman/Insomnia 测试并截图
- [ ] 前端已接入后端书籍接口
- [ ] 答题纸已补全项目结构说明和测试说明
- [ ] 前后端目录及辅助文件已一起打包

## 14. 一句话总目标

以当前 React 书店前端为基础，补齐一个结构合理的 Spring Boot + MySQL 后端，完成 3 个指定 API、前后端基础集成、接口测试和作业文档整理，并保证整个实现能自然衔接后续迭代 2。
